VECTOR_SIZE=5000000
$MY/sort  $VECTOR_SIZE $N_BUCKETS $PARALLEL $N_RUNS $PRINT_INFO

VECTOR_SIZE=1000000
$MY/sort  $VECTOR_SIZE $N_BUCKETS $PARALLEL $N_RUNS $PRINT_INFO

VECTOR_SIZE=500000
$MY/sort  $VECTOR_SIZE $N_BUCKETS $PARALLEL $N_RUNS $PRINT_INFO

VECTOR_SIZE=100000
$MY/sort  $VECTOR_SIZE $N_BUCKETS $PARALLEL $N_RUNS $PRINT_INFO

VECTOR_SIZE=50000
$MY/sort  $VECTOR_SIZE $N_BUCKETS $PARALLEL $N_RUNS $PRINT_INFO

VECTOR_SIZE=10000
$MY/sort  $VECTOR_SIZE $N_BUCKETS $PARALLEL $N_RUNS $PRINT_INFO

VECTOR_SIZE=5000
$MY/sort  $VECTOR_SIZE $N_BUCKETS $PARALLEL $N_RUNS $PRINT_INFO

VECTOR_SIZE=1000
$MY/sort  $VECTOR_SIZE $N_BUCKETS $PARALLEL $N_RUNS $PRINT_INFO